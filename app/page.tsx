import AppConnectButton from "../components/AppConnectButton";

function Page() {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "flex-end",
        padding: 12,
      }}
    >
      <AppConnectButton />
    </div>
  );
}

export default Page;
